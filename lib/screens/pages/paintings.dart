import 'package:flutter/material.dart';


class PaintingsPage extends StatelessWidget {
  const PaintingsPage({super.key});

  @override
   Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Paintings Page')
        ),
    );
  }
}